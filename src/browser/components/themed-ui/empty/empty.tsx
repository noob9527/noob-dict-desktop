import styled from 'styled-components';
import { Empty } from 'antd';

const ThemedEmpty = styled(Empty)`
  width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export { ThemedEmpty };
